# AppСтрой compatibility patch

Vendored from `file_saver` 0.4.0 under its BSD-3-Clause license.

The Web implementation uses the current `package:web` `URL.createObjectURL` and `URL.revokeObjectURL` APIs. No plugin business behavior was changed.
