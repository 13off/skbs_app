# Тотальная оптимизация AppСтрой — журнал выполнения

## Неприкосновенные контракты

Все пакеты этой программы обязаны сохранять без изменений:

- бизнес-логику и расчёты;
- роли, RLS, права и объектную изоляцию;
- маршруты, состав экранов и пользовательские сценарии;
- подписи, размеры, расположение элементов, отступы, цвета и типографику;
- форматы данных, RPC и Edge Function, если изменение контракта отдельно не согласовано;
- текущую совместимость Web/PWA, Android и desktop.

Каждый пакет выполняется отдельной веткой и проходит анализатор, тесты, release web-build и профильную проверку изменённого контура.

## Пакет 1 — синхронизация и кеши

Ветка: `agent/total-optimization-package-1`.

Статус: слит в `main`, PR #390.

### Выполнено

1. Realtime-события от уже отключённого канала больше не могут инициировать обновление активной компании.
2. При непрерывном потоке событий сохранено объединение изменений, но доставка теперь ограничена максимальным окном 500 мс и не может бесконечно откладываться.
3. Кеш кабинета сотрудника очищается один раз, даже когда один пакет одновременно затрагивает сотрудников и задачи.
4. Добавлены регрессионные тесты для зависимостей кешей и новых защитных контрактов.

### Проверки

Успешно пройдены Flutter analyzer, полный набор regression tests, release web-build, headless Chrome browser smoke и backend-check.

## Пакет 2 — повторные загрузки выплат

Ветка: `agent/total-optimization-package-2`.

Статус: слит в `main`, PR #391.

### Выполнено

1. Массовая загрузка выплат получила короткий кеш на 20 секунд.
2. Результат массового RPC прогревает индивидуальные кеши всех запрошенных сотрудников.
3. После открытия общего списка карточка отдельного сотрудника может использовать уже полученные данные без повторного запроса.
4. Сохранение, удаление выплаты и добавление чека по-прежнему очищают как индивидуальные, так и массовые кеши.
5. `forceRefresh` продолжает обходить готовый кеш.
6. Добавлен регрессионный контракт кеширования массовых выплат.

### Проверки

Успешно пройдены Flutter analyzer, полный набор regression tests, release web-build, headless Chrome browser smoke и backend-check.

## Пакет 3 — личные данные сотрудников

Ветка: `agent/total-optimization-package-3`.

### Выполнено

1. Свежий результат массовой загрузки личных данных прогревает индивидуальные кеши карточек сотрудников.
2. Для запрошенного сотрудника без строки `employee_private_data` кешируется отсутствие данных, чтобы не повторять одинаковый запрос при последовательном открытии экранов.
3. Массовая и индивидуальная записи используют единое время создания кеша и не продлевают TTL устаревшего ответа.
4. Полная массовая загрузка прогревает кеш только реально полученных сотрудников.
5. `forceRefresh`, чанки по 80 идентификаторов, параллельная загрузка и очистка после `upsert` сохранены.
6. Добавлен регрессионный контракт прогрева кеша личных данных.

### Что намеренно не изменено

- таблица `employee_private_data` и формат модели;
- набор выбираемых полей;
- размер чанка и параллельность запросов;
- правила RLS и доступ к персональным данным;
- обработка отсутствующих строк;
- форма редактирования и отображение карточек;
- срок жизни кеша 25 секунд.

### Обязательная проверка перед слиянием

```text
flutter pub get
flutter analyze
flutter test
flutter build web --release
```

Дополнительно проверить:

- массовое открытие списка сотрудников;
- последовательное открытие карточек сотрудников из этого списка;
- сотрудника с заполненными личными данными;
- сотрудника без строки личных данных;
- сохранение личных данных и немедленное повторное открытие;
- отсутствие изменений отображаемых значений и прав доступа.

## Следующие пакеты

1. Инструментальные замеры холодного и тёплого запуска, открытия вкладок и тяжёлых списков.
2. Аудит оставшихся eager-списков и перевод только подтверждённых длинных списков на ленивую отрисовку.
3. Аудит повторных запросов остальных репозиториев и точечное in-flight объединение без увеличения TTL.
4. Оптимизация превью изображений, файлов и экспортов с контролем памяти.
5. Анализ production-запросов и индексов только по фактической статистике.
6. Чистка dead code и зависимостей только после доказательства отсутствия использования и полного регресса.
