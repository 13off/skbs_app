name: Build Android APK

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - release/android-release.txt

permissions:
  contents: write

concurrency:
  group: appstroy-android-apk
  cancel-in-progress: true

env:
  FIREBASE_API_KEY: ${{ vars.FIREBASE_API_KEY }}
  FIREBASE_PROJECT_ID: ${{ vars.FIREBASE_PROJECT_ID }}
  FIREBASE_MESSAGING_SENDER_ID: ${{ vars.FIREBASE_MESSAGING_SENDER_ID }}
  FIREBASE_AUTH_DOMAIN: ${{ vars.FIREBASE_AUTH_DOMAIN }}
  FIREBASE_STORAGE_BUCKET: ${{ vars.FIREBASE_STORAGE_BUCKET }}
  FIREBASE_WEB_APP_ID: ${{ vars.FIREBASE_WEB_APP_ID }}
  FIREBASE_ANDROID_APP_ID: ${{ vars.FIREBASE_ANDROID_APP_ID }}
  FIREBASE_IOS_APP_ID: ${{ vars.FIREBASE_IOS_APP_ID }}
  FIREBASE_VAPID_KEY: ${{ vars.FIREBASE_VAPID_KEY }}

jobs:
  build-apk:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout source
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      - name: Setup Java 17
        uses: actions/setup-java@cf277c60eb25467037889841efdb72551f06f6c3 # v4
        with:
          distribution: temurin
          java-version: '17'
          cache: gradle

      - name: Setup Flutter
        uses: subosito/flutter-action@1a449444c387b1966244ae4d4f8c696479add0b2 # v2
        with:
          flutter-version-file: .fvmrc
          cache: true

      - name: Install locked dependencies
        run: flutter pub get --enforce-lockfile

      - name: Run regression tests for manual releases
        if: github.event_name == 'workflow_dispatch'
        run: flutter test

      - name: Analyze source for manual releases
        if: github.event_name == 'workflow_dispatch'
        run: flutter analyze --no-fatal-infos --no-fatal-warnings

      - name: Build release APK through proxy
        id: apk_build
        shell: bash
        run: |
          set +e
          flutter build apk --release \
            --dart-define=SUPABASE_URL=https://api.appstroy-web.ru \
            --dart-define=FIREBASE_API_KEY="$FIREBASE_API_KEY" \
            --dart-define=FIREBASE_PROJECT_ID="$FIREBASE_PROJECT_ID" \
            --dart-define=FIREBASE_MESSAGING_SENDER_ID="$FIREBASE_MESSAGING_SENDER_ID" \
            --dart-define=FIREBASE_AUTH_DOMAIN="$FIREBASE_AUTH_DOMAIN" \
            --dart-define=FIREBASE_STORAGE_BUCKET="$FIREBASE_STORAGE_BUCKET" \
            --dart-define=FIREBASE_WEB_APP_ID="$FIREBASE_WEB_APP_ID" \
            --dart-define=FIREBASE_ANDROID_APP_ID="$FIREBASE_ANDROID_APP_ID" \
            --dart-define=FIREBASE_IOS_APP_ID="$FIREBASE_IOS_APP_ID" \
            --dart-define=FIREBASE_VAPID_KEY="$FIREBASE_VAPID_KEY" \
            2>&1 | tee apk-build.log
          BUILD_EXIT_CODE=${PIPESTATUS[0]}

          APK_PATH="build/app/outputs/flutter-apk/app-release.apk"
          if [ "$BUILD_EXIT_CODE" -ne 0 ] && [ -s "$APK_PATH" ]; then
            echo "::warning::Flutter returned ${BUILD_EXIT_CODE}, but a non-empty release APK was produced. Treating the build as successful."
            printf '\nRelease APK exists and is non-empty; overriding the Flutter wrapper exit code.\n' >> apk-build.log
            BUILD_EXIT_CODE=0
          fi

          if [ "$BUILD_EXIT_CODE" -eq 0 ] && [ ! -s "$APK_PATH" ]; then
            echo "::error::Flutter reported success, but release APK is missing or empty"
            printf '\nRelease APK is missing or empty.\n' >> apk-build.log
            BUILD_EXIT_CODE=1
          fi

          echo "exit_code=${BUILD_EXIT_CODE}" >> "$GITHUB_OUTPUT"
          exit 0

      - name: Prepare APK files
        if: steps.apk_build.outputs.exit_code == '0'
        shell: bash
        run: |
          SOURCE_SHA="$(git rev-parse HEAD)"
          SHORT_SHA="$(git rev-parse --short=12 HEAD)"
          mkdir -p apk-output
          cp build/app/outputs/flutter-apk/app-release.apk "apk-output/AppStroy-${SHORT_SHA}.apk"
          cp build/app/outputs/flutter-apk/app-release.apk apk-output/AppStroy-latest.apk
          printf '%s\n' "$SOURCE_SHA" > apk-output/source-commit.txt
          sha256sum apk-output/AppStroy-latest.apk > apk-output/AppStroy-latest.sha256

      - name: Publish APK in GitHub Release
        if: steps.apk_build.outputs.exit_code == '0'
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          TAG="appstroy-latest"
          if ! gh release view "$TAG" >/dev/null 2>&1; then
            gh release create "$TAG" \
              --target "$GITHUB_SHA" \
              --title "AppСтрой — актуальные сборки" \
              --notes "Автоматически обновляемые сборки AppСтрой для Android, Windows и iOS." \
              || gh release view "$TAG" >/dev/null
          fi
          gh release upload "$TAG" \
            apk-output/AppStroy-latest.apk \
            apk-output/AppStroy-latest.sha256 \
            apk-output/source-commit.txt \
            --clobber

      - name: Fail workflow when APK build failed
        if: steps.apk_build.outputs.exit_code != '0'
        run: exit 1
