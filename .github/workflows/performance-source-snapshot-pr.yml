name: Performance source snapshot PR

on:
  pull_request:
    branches:
      - main
    paths:
      - tool/performance_snapshot.trigger
      - .github/workflows/performance-source-snapshot-pr.yml

permissions:
  contents: read

jobs:
  snapshot:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Create clean source archive
        run: |
          tar \
            --exclude=.git \
            --exclude=.dart_tool \
            --exclude=build \
            --exclude='**/build' \
            --exclude='**/.gradle' \
            --exclude='**/Pods' \
            -czf skbs-app-source.tar.gz .

      - name: Upload source snapshot
        uses: actions/upload-artifact@v4
        with:
          name: skbs-app-performance-source
          path: skbs-app-source.tar.gz
          if-no-files-found: error
          retention-days: 1
