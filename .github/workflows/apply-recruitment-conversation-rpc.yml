name: Apply recruitment conversation routing

on:
  pull_request:
    branches:
      - main

permissions:
  contents: write

jobs:
  patch-and-check:
    if: github.head_ref == 'fix/recruitment-image-viewer-inbound-chat' && github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0

      - name: Apply routing RPC
        run: python3 tool/apply_recruitment_conversation_rpc.py

      - uses: subosito/flutter-action@v2
        with:
          flutter-version-file: .fvmrc
          cache: true

      - name: Run verification
        shell: bash
        run: |
          dart format \
            lib/features/recruitment/data/recruitment_repository.dart \
            test/recruitment_documents_chat_contract_test.dart
          flutter pub get
          flutter test
          flutter analyze --no-fatal-infos --no-fatal-warnings
          flutter build web --release --base-href /appstroy-web/

      - name: Commit verified routing
        shell: bash
        run: |
          rm tool/apply_recruitment_conversation_rpc.py
          rm .github/workflows/apply-recruitment-conversation-rpc.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Привязать ответ кандидата к активной заявке"
          git push origin HEAD:fix/recruitment-image-viewer-inbound-chat
