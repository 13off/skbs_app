name: Apply add payment dark theme patch

on:
  pull_request:
    branches:
      - main
    paths:
      - tool/add_payment_dark_theme.trigger

permissions:
  contents: write

concurrency:
  group: appstroy-add-payment-dark-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  patch-and-validate:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: true

      - name: Prepare pull request branch
        run: git switch -C "${GITHUB_HEAD_REF}"

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version-file: .fvmrc
          cache: true

      - name: Install locked dependencies
        run: flutter pub get --enforce-lockfile

      - name: Apply color patch
        run: python3 tool/apply_add_payment_dark_theme.py

      - name: Format sources
        run: |
          dart format \
            lib/screens/add_payment_screen.dart \
            test/add_payment_dark_theme_contract_test.dart

      - name: Run targeted contract
        id: targeted
        shell: bash
        run: |
          set +e
          flutter test test/add_payment_dark_theme_contract_test.dart > targeted-test.log 2>&1
          status=$?
          cat targeted-test.log
          echo "status=$status" >> "$GITHUB_OUTPUT"
          exit 0

      - name: Upload targeted test log
        if: steps.targeted.outputs.status != '0'
        uses: actions/upload-artifact@v4
        with:
          name: add-payment-dark-targeted-test-log
          path: targeted-test.log
          if-no-files-found: error
          retention-days: 3

      - name: Fail on targeted test errors
        if: steps.targeted.outputs.status != '0'
        run: exit 1

      - name: Analyze source
        id: analyzer
        shell: bash
        run: |
          set +e
          flutter analyze --no-fatal-infos --no-fatal-warnings > analyzer.log 2>&1
          status=$?
          cat analyzer.log
          echo "status=$status" >> "$GITHUB_OUTPUT"
          exit 0

      - name: Upload analyzer log
        if: steps.analyzer.outputs.status != '0'
        uses: actions/upload-artifact@v4
        with:
          name: add-payment-dark-analyzer-log
          path: analyzer.log
          if-no-files-found: error
          retention-days: 3

      - name: Fail on analyzer errors
        if: steps.analyzer.outputs.status != '0'
        run: exit 1

      - name: Remove temporary patch files
        run: |
          rm -f .github/workflows/apply-add-payment-dark-theme.yml
          rm -f tool/apply_add_payment_dark_theme.py
          rm -f tool/add_payment_dark_theme.trigger
          rm -f targeted-test.log analyzer.log

      - name: Commit validated patch
        uses: stefanzweifel/git-auto-commit-action@v6
        with:
          branch: ${{ github.head_ref }}
          commit_message: исправлена тёмная тема добавления выплаты
          file_pattern: .
