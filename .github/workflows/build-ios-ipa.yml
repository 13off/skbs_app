name: Build iOS IPA

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - ios/**
      - lib/**
      - test/**
      - pubspec.yaml
      - pubspec.lock
      - .fvmrc
      - .github/workflows/build-ios-ipa.yml

permissions:
  contents: read

concurrency:
  group: appstroy-ios-ipa
  cancel-in-progress: true

env:
  FIREBASE_API_KEY: ${{ vars.FIREBASE_API_KEY }}
  FIREBASE_PROJECT_ID: ${{ vars.FIREBASE_PROJECT_ID }}
  FIREBASE_MESSAGING_SENDER_ID: ${{ vars.FIREBASE_MESSAGING_SENDER_ID }}
  FIREBASE_AUTH_DOMAIN: ${{ vars.FIREBASE_AUTH_DOMAIN }}
  FIREBASE_STORAGE_BUCKET: ${{ vars.FIREBASE_STORAGE_BUCKET }}
  FIREBASE_WEB_APP_ID: ${{ vars.FIREBASE_WEB_APP_ID }}
  FIREBASE_ANDROID_APP_ID: ${{ vars.FIREBASE_ANDROID_APP_ID }}
  FIREBASE_IOS_APP_ID: ${{ vars.FIREBASE_IOS_APP_ID }}
  FIREBASE_VAPID_KEY: ${{ vars.FIREBASE_VAPID_KEY }}

jobs:
  build-ios-ipa:
    runs-on: macos-latest

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Checkout download repository
        uses: actions/checkout@v4
        with:
          repository: 13off/appstroy-web
          token: ${{ secrets.WEB_DEPLOY_TOKEN }}
          path: appstroy-web

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version-file: .fvmrc
          cache: true

      - name: Install locked dependencies
        run: flutter pub get --enforce-lockfile

      - name: Run regression tests
        run: flutter test

      - name: Analyze source
        run: flutter analyze --no-fatal-infos --no-fatal-warnings

      - name: Prepare iOS app
        shell: bash
        run: |
          perl -0pi -e 's/com\.example\.skbsApp/ru.appstroy.mobile/g' ios/Runner.xcodeproj/project.pbxproj
          python3 - <<'PY'
          from pathlib import Path
          project = Path('ios/Runner.xcodeproj/project.pbxproj')
          text = project.read_text(encoding='utf-8')
          if 'CODE_SIGN_ENTITLEMENTS = Runner/Runner.entitlements;' not in text:
              text = text.replace(
                  'INFOPLIST_FILE = Runner/Info.plist;',
                  'CODE_SIGN_ENTITLEMENTS = Runner/Runner.entitlements;\n\t\t\t\tINFOPLIST_FILE = Runner/Info.plist;',
              )
          project.write_text(text, encoding='utf-8')
          PY
          grep -q 'PRODUCT_BUNDLE_IDENTIFIER = ru.appstroy.mobile;' ios/Runner.xcodeproj/project.pbxproj
          grep -q 'CODE_SIGN_ENTITLEMENTS = Runner/Runner.entitlements;' ios/Runner.xcodeproj/project.pbxproj
          /usr/libexec/PlistBuddy -c "Set :CFBundleDisplayName AppСтрой" ios/Runner/Info.plist
          /usr/libexec/PlistBuddy -c "Set :CFBundleName AppStroy" ios/Runner/Info.plist

      - name: Build unsigned app
        id: ios_build
        continue-on-error: true
        shell: bash
        run: |
          set -o pipefail
          flutter build ios --release --no-codesign \
            --dart-define=SUPABASE_URL=https://api.appstroy-web.ru \
            --dart-define=FIREBASE_API_KEY="$FIREBASE_API_KEY" \
            --dart-define=FIREBASE_PROJECT_ID="$FIREBASE_PROJECT_ID" \
            --dart-define=FIREBASE_MESSAGING_SENDER_ID="$FIREBASE_MESSAGING_SENDER_ID" \
            --dart-define=FIREBASE_AUTH_DOMAIN="$FIREBASE_AUTH_DOMAIN" \
            --dart-define=FIREBASE_STORAGE_BUCKET="$FIREBASE_STORAGE_BUCKET" \
            --dart-define=FIREBASE_WEB_APP_ID="$FIREBASE_WEB_APP_ID" \
            --dart-define=FIREBASE_ANDROID_APP_ID="$FIREBASE_ANDROID_APP_ID" \
            --dart-define=FIREBASE_IOS_APP_ID="$FIREBASE_IOS_APP_ID" \
            --dart-define=FIREBASE_VAPID_KEY="$FIREBASE_VAPID_KEY" \
            2>&1 | tee ios-build.log

      - name: Create IPA
        if: steps.ios_build.outcome == 'success'
        shell: bash
        run: |
          SHORT_SHA="$(git rev-parse --short=12 HEAD)"
          mkdir -p ios-output/Payload
          cp -R build/ios/iphoneos/Runner.app ios-output/Payload/AppStroy.app
          cd ios-output
          zip -qry "AppStroy-${SHORT_SHA}.ipa" Payload
          cp "AppStroy-${SHORT_SHA}.ipa" AppStroy-latest.ipa
          rm -rf Payload
          shasum -a 256 AppStroy-latest.ipa > AppStroy-iOS-latest.sha256

      - name: Upload IPA artifact
        if: steps.ios_build.outcome == 'success'
        uses: actions/upload-artifact@v4
        with:
          name: AppStroy-iOS-IPA
          path: ios-output/
          retention-days: 30

      - name: Publish final iOS build result
        if: always()
        shell: bash
        run: |
          cd appstroy-web
          git pull --rebase
          cd ..
          mkdir -p appstroy-web/downloads
          cp ios-build.log appstroy-web/downloads/ios-build.log || true
          printf '%s\n' "$(git rev-parse HEAD)" > appstroy-web/downloads/ios-source-commit.txt

          if [ "${{ steps.ios_build.outcome }}" = "success" ]; then
            rm -f appstroy-web/downloads/AppStroy-*.ipa
            cp ios-output/AppStroy-*.ipa appstroy-web/downloads/
            cp ios-output/AppStroy-iOS-latest.sha256 appstroy-web/downloads/
            printf 'success\n' > appstroy-web/downloads/ios-build-status.txt
          else
            printf 'failure\n' > appstroy-web/downloads/ios-build-status.txt
          fi

          cd appstroy-web
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add downloads
          if ! git diff --cached --quiet; then
            if [ "${{ steps.ios_build.outcome }}" = "success" ]; then
              git commit -m "опубликована проверочная IPA для iPhone"
            else
              git commit -m "сохранена ошибка сборки iOS"
            fi
            git pull --rebase
            git push
          fi

      - name: Fail workflow when iOS build failed
        if: steps.ios_build.outcome != 'success'
        run: exit 1
