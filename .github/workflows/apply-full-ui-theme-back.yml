name: Apply full UI theme and back patch

on:
  push:
    branches:
      - agent/full-ui-theme-back-clean
    paths:
      - tool/fix_full_ui_const_conflicts.py
      - .github/workflows/apply-full-ui-theme-back.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/full-ui-theme-back-clean
          fetch-depth: 0

      - name: Fix dynamic const conflicts
        run: python3 tool/fix_full_ui_const_conflicts.py

      - name: Commit targeted changes
        shell: bash
        run: |
          mapfile -t files < <(git diff --name-only --diff-filter=ACM)
          if [ ${#files[@]} -eq 0 ]; then
            echo "No changes to commit"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add "${files[@]}"
          git commit -m "исправлены const-конфликты полной UI-темы"
          git push origin HEAD:agent/full-ui-theme-back-clean
