name: Validate AI assistant MVP

on:
  push:
    branches:
      - feature/ai-assistant-mvp
    paths:
      - .github/workflows/validate-ai-assistant-mvp.yml

permissions:
  contents: read

jobs:
  test-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/ai-assistant-mvp
      - uses: subosito/flutter-action@v2
        with:
          flutter-version-file: .fvmrc
          cache: true
      - run: flutter pub get --enforce-lockfile
      - run: flutter test
      - run: flutter analyze --no-fatal-infos --no-fatal-warnings
      - run: flutter build web --release --base-href /appstroy-web/ --dart-define=SUPABASE_URL=https://api.appstroy-web.ru
