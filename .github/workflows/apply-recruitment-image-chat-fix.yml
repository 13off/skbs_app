name: Apply recruitment image and chat fix

on:
  pull_request:
    branches:
      - main

permissions:
  contents: write

jobs:
  patch-and-check:
    if: github.head_ref == 'fix/recruitment-image-viewer-inbound-chat' && github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0

      - name: Apply fix
        run: python3 tool/apply_recruitment_image_chat_fix.py

      - uses: subosito/flutter-action@v2
        with:
          flutter-version-file: .fvmrc
          cache: true

      - name: Run verification
        shell: bash
        run: |
          dart format \
            lib/features/recruitment/presentation/recruitment_application_detail_screen.dart \
            test/recruitment_documents_chat_contract_test.dart
          flutter pub get
          flutter test
          flutter analyze --no-fatal-infos --no-fatal-warnings
          flutter build web --release --base-href /appstroy-web/

      - name: Commit verified changes
        shell: bash
        run: |
          rm tool/apply_recruitment_image_chat_fix.py
          rm .github/workflows/apply-recruitment-image-chat-fix.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Исправить просмотр фото и входящие сообщения кандидата"
          git push origin HEAD:fix/recruitment-image-viewer-inbound-chat
