name: MAX recruiting bot temporarily disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Keep MAX bot disabled
        run: |
          echo "MAX recruiting bot is temporarily disabled."
          echo "VPS deployment is intentionally blocked until the bot is re-enabled."
